-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.aplicacion (
  id integer NOT NULL DEFAULT nextval('aplicacion_id_seq'::regclass),
  nombre character varying NOT NULL,
  CONSTRAINT aplicacion_pkey PRIMARY KEY (id)
);
CREATE TABLE public.estado (
  id integer NOT NULL DEFAULT nextval('estado_id_seq'::regclass),
  nombre character varying NOT NULL,
  CONSTRAINT estado_pkey PRIMARY KEY (id)
);
CREATE TABLE public.estado_jira (
  id integer NOT NULL DEFAULT nextval('estado_jira_id_seq'::regclass),
  nombre character varying NOT NULL,
  CONSTRAINT estado_jira_pkey PRIMARY KEY (id)
);
CREATE TABLE public.feriados (
  id integer NOT NULL DEFAULT nextval('feriados_id_seq'::regclass),
  fecha date NOT NULL UNIQUE,
  descripcion text,
  CONSTRAINT feriados_pkey PRIMARY KEY (id)
);
CREATE TABLE public.prioridad (
  id integer NOT NULL DEFAULT nextval('prioridad_id_seq'::regclass),
  nombre character varying NOT NULL,
  CONSTRAINT prioridad_pkey PRIMARY KEY (id)
);
CREATE TABLE public.producto (
  id integer NOT NULL DEFAULT nextval('producto_id_seq'::regclass),
  nombre character varying NOT NULL,
  CONSTRAINT producto_pkey PRIMARY KEY (id)
);
CREATE TABLE public.tareo_agrupador (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  nombre character varying NOT NULL UNIQUE,
  activo boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT tareo_agrupador_pkey PRIMARY KEY (id)
);
CREATE TABLE public.tareo_ajuste (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  tarea_id bigint NOT NULL,
  tipo character varying NOT NULL,
  horas_antes numeric,
  horas_despues numeric,
  descripcion text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT tareo_ajuste_pkey PRIMARY KEY (id),
  CONSTRAINT tareo_ajuste_tarea_id_fkey FOREIGN KEY (tarea_id) REFERENCES public.tareo_tarea(id)
);
CREATE TABLE public.tareo_comentario_draft (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  link_id uuid NOT NULL,
  tarea_periodo_id integer NOT NULL,
  comentario_dm text NOT NULL,
  estado character varying DEFAULT 'Pendiente'::character varying,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT tareo_comentario_draft_pkey PRIMARY KEY (id),
  CONSTRAINT tareo_comentario_draft_link_id_fkey FOREIGN KEY (link_id) REFERENCES public.tareo_reporte_link(id),
  CONSTRAINT tareo_comentario_draft_tarea_periodo_id_fkey FOREIGN KEY (tarea_periodo_id) REFERENCES public.tareo_tarea_periodo(id)
);
CREATE TABLE public.tareo_comentarios_externos (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  referencia_tipo character varying NOT NULL,
  referencia_id integer NOT NULL,
  comentario_protecta text,
  comentario_dev text,
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT tareo_comentarios_externos_pkey PRIMARY KEY (id)
);
CREATE TABLE public.tareo_delegacion (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  fecha date NOT NULL,
  tarea_id bigint NOT NULL,
  trabajador_origen_id bigint NOT NULL,
  trabajador_destino_id bigint NOT NULL,
  horas numeric NOT NULL CHECK (horas > 0::numeric AND horas <= 12::numeric),
  motivo text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT tareo_delegacion_pkey PRIMARY KEY (id),
  CONSTRAINT tareo_delegacion_tarea_id_fkey FOREIGN KEY (tarea_id) REFERENCES public.tareo_tarea(id),
  CONSTRAINT tareo_delegacion_trabajador_origen_id_fkey FOREIGN KEY (trabajador_origen_id) REFERENCES public.tareo_trabajador(id),
  CONSTRAINT tareo_delegacion_trabajador_destino_id_fkey FOREIGN KEY (trabajador_destino_id) REFERENCES public.tareo_trabajador(id)
);
CREATE TABLE public.tareo_estado_tarea (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  nombre character varying NOT NULL UNIQUE,
  activo boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT tareo_estado_tarea_pkey PRIMARY KEY (id)
);
CREATE TABLE public.tareo_historial (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  entidad character varying NOT NULL,
  entidad_id bigint NOT NULL,
  accion character varying NOT NULL,
  descripcion text,
  fecha_movimiento timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT tareo_historial_pkey PRIMARY KEY (id)
);
CREATE TABLE public.tareo_periodo (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  anio integer NOT NULL,
  mes integer NOT NULL CHECK (mes >= 1 AND mes <= 12),
  fecha_inicio date NOT NULL,
  fecha_fin date NOT NULL,
  cerrado boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT tareo_periodo_pkey PRIMARY KEY (id)
);
CREATE TABLE public.tareo_proyecto (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  agrupador_id bigint NOT NULL,
  nombre character varying NOT NULL,
  activo boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT tareo_proyecto_pkey PRIMARY KEY (id),
  CONSTRAINT tareo_proyecto_agrupador_id_fkey FOREIGN KEY (agrupador_id) REFERENCES public.tareo_agrupador(id)
);
CREATE TABLE public.tareo_registro (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  tarea_id bigint,
  fecha date NOT NULL,
  trabajador_id bigint NOT NULL,
  horas numeric NOT NULL CHECK (horas > 0::numeric AND horas <= 12::numeric),
  comentario text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  tarea_periodo_id bigint NOT NULL,
  CONSTRAINT tareo_registro_pkey PRIMARY KEY (id),
  CONSTRAINT tareo_registro_tarea_id_fkey FOREIGN KEY (tarea_id) REFERENCES public.tareo_tarea(id),
  CONSTRAINT tareo_registro_trabajador_id_fkey FOREIGN KEY (trabajador_id) REFERENCES public.tareo_trabajador(id),
  CONSTRAINT tareo_registro_tarea_periodo_id_fkey FOREIGN KEY (tarea_periodo_id) REFERENCES public.tareo_tarea_periodo(id)
);
CREATE TABLE public.tareo_reporte_link (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  periodo_id integer NOT NULL,
  token text NOT NULL DEFAULT encode(gen_random_bytes(16), 'hex'::text) UNIQUE,
  fecha_expiracion timestamp with time zone NOT NULL,
  activo boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT tareo_reporte_link_pkey PRIMARY KEY (id),
  CONSTRAINT tareo_reporte_link_periodo_id_fkey FOREIGN KEY (periodo_id) REFERENCES public.tareo_periodo(id)
);
CREATE TABLE public.tareo_solicitante (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  nombre character varying NOT NULL UNIQUE,
  horas_maximas_estimadas numeric,
  activo boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT tareo_solicitante_pkey PRIMARY KEY (id)
);
CREATE TABLE public.tareo_tarea (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  periodo_id bigint NOT NULL,
  nombre character varying NOT NULL,
  proyecto_id bigint NOT NULL,
  team_id bigint,
  solicitante_id bigint NOT NULL,
  estado_id bigint NOT NULL,
  horas_totales numeric NOT NULL CHECK (horas_totales > 0::numeric),
  horas_consumidas numeric NOT NULL DEFAULT 0 CHECK (horas_consumidas >= 0::numeric),
  horas_disponibles numeric DEFAULT (horas_totales - horas_consumidas),
  comentario_ps text,
  activo boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  comentario_dm text,
  CONSTRAINT tareo_tarea_pkey PRIMARY KEY (id),
  CONSTRAINT tareo_tarea_proyecto_id_fkey FOREIGN KEY (proyecto_id) REFERENCES public.tareo_proyecto(id),
  CONSTRAINT tareo_tarea_team_id_fkey FOREIGN KEY (team_id) REFERENCES public.tareo_team(id),
  CONSTRAINT tareo_tarea_solicitante_id_fkey FOREIGN KEY (solicitante_id) REFERENCES public.tareo_solicitante(id),
  CONSTRAINT tareo_tarea_estado_id_fkey FOREIGN KEY (estado_id) REFERENCES public.tareo_estado_tarea(id)
);
CREATE TABLE public.tareo_tarea_periodo (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  tarea_id bigint NOT NULL,
  periodo_id bigint NOT NULL,
  horas_historicas_arrastre numeric NOT NULL DEFAULT 0 CHECK (horas_historicas_arrastre >= 0::numeric),
  horas_asignadas_periodo numeric NOT NULL DEFAULT 0 CHECK (horas_asignadas_periodo >= 0::numeric),
  horas_consumidas_periodo numeric NOT NULL DEFAULT 0 CHECK (horas_consumidas_periodo >= 0::numeric),
  horas_disponibles_periodo numeric DEFAULT (horas_asignadas_periodo - horas_consumidas_periodo),
  comentario_periodo text,
  activo boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT tareo_tarea_periodo_pkey PRIMARY KEY (id),
  CONSTRAINT tareo_tarea_periodo_tarea_id_fkey FOREIGN KEY (tarea_id) REFERENCES public.tareo_tarea(id),
  CONSTRAINT tareo_tarea_periodo_periodo_id_fkey FOREIGN KEY (periodo_id) REFERENCES public.tareo_periodo(id)
);
CREATE TABLE public.tareo_team (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  nombre character varying NOT NULL UNIQUE,
  activo boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT tareo_team_pkey PRIMARY KEY (id)
);
CREATE TABLE public.tareo_trabajador (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  nombre character varying NOT NULL UNIQUE,
  activo boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  correo character varying,
  telefono character varying,
  CONSTRAINT tareo_trabajador_pkey PRIMARY KEY (id)
);
CREATE TABLE public.ticket_gestion (
  ticket_id integer NOT NULL,
  producto_id integer,
  aplicacion_id integer,
  poliza character varying,
  dni character varying,
  comentario text,
  horas_invertidas numeric,
  observaciones text,
  tipo_sd character varying,
  tipo_sd_id integer,
  CONSTRAINT ticket_gestion_pkey PRIMARY KEY (ticket_id),
  CONSTRAINT ticket_gestion_ticket_id_fkey FOREIGN KEY (ticket_id) REFERENCES public.tickets(id),
  CONSTRAINT ticket_gestion_producto_id_fkey FOREIGN KEY (producto_id) REFERENCES public.producto(id),
  CONSTRAINT ticket_gestion_aplicacion_id_fkey FOREIGN KEY (aplicacion_id) REFERENCES public.aplicacion(id)
);
CREATE TABLE public.ticket_historial (
  id integer NOT NULL DEFAULT nextval('ticket_historial_id_seq'::regclass),
  ticket_id integer NOT NULL,
  usuario_id integer NOT NULL,
  fecha_movimiento timestamp with time zone DEFAULT now(),
  accion character varying NOT NULL,
  descripcion_cambio text,
  CONSTRAINT ticket_historial_pkey PRIMARY KEY (id),
  CONSTRAINT ticket_historial_ticket_id_fkey FOREIGN KEY (ticket_id) REFERENCES public.tickets(id),
  CONSTRAINT ticket_historial_usuario_id_fkey FOREIGN KEY (usuario_id) REFERENCES public.usuarios(id)
);
CREATE TABLE public.ticket_tiempos (
  ticket_id integer NOT NULL,
  fecha_registro timestamp with time zone,
  fecha_asignacion timestamp with time zone,
  fecha_maxima_atencion timestamp with time zone,
  fecha_atencion timestamp with time zone,
  fecha_delegacion timestamp with time zone,
  fecha_creacion_sd timestamp with time zone,
  CONSTRAINT ticket_tiempos_pkey PRIMARY KEY (ticket_id),
  CONSTRAINT ticket_tiempos_ticket_id_fkey FOREIGN KEY (ticket_id) REFERENCES public.tickets(id)
);
CREATE TABLE public.tickets (
  id integer NOT NULL DEFAULT nextval('tickets_id_seq'::regclass),
  numero_ticket integer NOT NULL UNIQUE,
  descripcion character varying NOT NULL,
  prioridad_id integer,
  estado_id integer,
  estado_jira_id integer,
  creador_id integer,
  responsable_id integer,
  horario_laboral character varying,
  CONSTRAINT tickets_pkey PRIMARY KEY (id),
  CONSTRAINT tickets_prioridad_id_fkey FOREIGN KEY (prioridad_id) REFERENCES public.prioridad(id),
  CONSTRAINT tickets_estado_id_fkey FOREIGN KEY (estado_id) REFERENCES public.estado(id),
  CONSTRAINT tickets_estado_jira_id_fkey FOREIGN KEY (estado_jira_id) REFERENCES public.estado_jira(id),
  CONSTRAINT tickets_creador_id_fkey FOREIGN KEY (creador_id) REFERENCES public.usuarios(id),
  CONSTRAINT tickets_responsable_id_fkey FOREIGN KEY (responsable_id) REFERENCES public.usuarios(id)
);
CREATE TABLE public.tipo_sd (
  id integer GENERATED ALWAYS AS IDENTITY NOT NULL,
  nombre character varying NOT NULL,
  CONSTRAINT tipo_sd_pkey PRIMARY KEY (id)
);
CREATE TABLE public.usuarios (
  id integer NOT NULL DEFAULT nextval('usuarios_id_seq'::regclass),
  nombre character varying NOT NULL,
  email character varying NOT NULL UNIQUE,
  password character varying NOT NULL,
  rol character varying DEFAULT 'Agente'::character varying,
  activo boolean DEFAULT true,
  fecha_registro timestamp with time zone DEFAULT now(),
  horario_laboral character varying DEFAULT '09:00 - 18:00'::character varying,
  CONSTRAINT usuarios_pkey PRIMARY KEY (id)
);